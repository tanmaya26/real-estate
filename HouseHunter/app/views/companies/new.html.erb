<h1>You are at company page</h1>
<% if current_user %>
  <% "Hello #{current_user.role_type} #{current_user.user_name}" %>
  <% if User.find(session[:id]).role_type == "admin" || User.find(session[:id]).role_type == "realtor"%>
<%= render 'form', company: @company %>
  <%else%>
    <h3> You don't have permission to add company. </h3>
  <% end %>
  <% end %>
<%= link_to 'Back', companies_path %>

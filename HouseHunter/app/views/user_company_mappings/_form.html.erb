<%= form_with(model: user_company_mapping, local: true) do |form| %>
  <% if user_company_mapping.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_company_mapping.errors.count, "error") %> prohibited this user_company_mapping from being saved:</h2>

      <ul>
      <% user_company_mapping.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :company_id %>
    <%= form.text_field :company_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
